# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# env / secrets
.env
.env.local
.env*.local

# local JSON booking store (dev only)
/data

# local tooling / source assets (not part of the deployed app)
.claude
photo and video recources

# misc
.DS_Store
*.pem
npm-debug.log*
.vercel
*.tsbuildinfo
next-env.d.ts

# handoff doc with secrets/notes — never commit
TRANSFER.md

# local dev photo uploads (Vercel Blob in prod)
/public/uploads
